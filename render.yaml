services:
  - type: web
    name: availabl-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python render_backend.py
    healthCheckPath: /health
    pullRequestPreviewsEnabled: false

previews:
  generation: automatic
